@page "/manager-only"
@using BlazorKeycloak
@using BlazorKeycloak.Client
@using BlazorKeycloak.Components
@using BlazorKeycloak.Components.Layout
@using Microsoft.AspNetCore.Authorization
@attribute [Authorize(Roles = "Manager")]

@rendermode InteractiveServer

@* 
// Раскомментируйте ОДНУ из следующих строк для теста:
// @rendermode InteractiveServer 
// @rendermode InteractiveWebAssembly
// (если ничего не указано, будет статический SSR)
 *@
<h3>Manager-Only Area</h3>

<p>If you see this, you are a Manager!</p>